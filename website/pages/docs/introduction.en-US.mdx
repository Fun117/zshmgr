---
title: Introduction
---

import { Callout, Steps, FileTree, Tabs, Cards, Card } from 'nextra/components'
import { Package } from 'lucide-react';

# Introduction

`zshmgr` is a simple package manager for Zsh. It allows you to install, uninstall, update, and list packages.

### Dependencies

`zshmgr` depends on the following commands, so please make sure they are installed:

- [git](https://git-scm.com/downloads)
- [jq](https://jqlang.github.io/jq/download/)

<Tabs items={["Windows", "macOS", "Linux"]}>
  <Tabs.Tab>
    <Callout type="warning" emoji="⚠️">
      For Windows, you need to use `WSL` to run `zshmgr`.
    </Callout>
  </Tabs.Tab>
  <Tabs.Tab>
    <></>
  </Tabs.Tab>
  <Tabs.Tab>
    <></>
  </Tabs.Tab>
</Tabs>

## Installation

### Installation Steps

### Download and run the `install.sh` script:

```bash
curl -O https://raw.githubusercontent.com/fun117/zshmgr/main/install.sh
chmod +x install.sh
./install.sh
```

Now, `zshmgr` is installed and available system-wide.

## License

This project is released under the MIT License. For more details, please refer to the LICENSE file.